Primero seleccionaremos la opcion git branch test-1 para crear una rama que reflejara el extremo de una serie de confirmaciones. 
A continuaci√≥n, seleccionamos este rama reci√©n creada usando el comando git checkout test-1, que apuntar√° hacia ella. 
Realizamos unos cambios en el repositorio local, git add. + git commit, y estos estar√≠an listos para ser subidos al repositorio remoto.
git push origin test-1 nos enviar√° los cambios realizados a nuestro remoto.
git checkout master nos avisa de que rama test-1 est√° por delante de master en lo que se refiere a cambios. 
Al intentar hacer un git push con cambios realizados en la rama master, nos encontraremos con un error: failed to push some refs to 'https://github.com/marcusskelly/GitResolucionConflictos'
<<<<<<< HEAD
Mediante un git pull en el repositorio local, nos dar· la opcion de "merge by recursive strategy" ya que al haber realizado un git push desde la rama test-1, est· habÌa sido subida al remoto, con contenido editado previamente. El problema esque despuÈs nuestro documento de texto no se encontraba en el repositorio local, por eso hemos tenido que realizar un pull primero, para tenerlos todos sincronizados.
Una vez realizado esto, nuestros commit en rama master pueden ser subidos al repositorio remoto mediante git push. 
=======
<<<<<<< HEAD
Mediante un git pull en el repositorio local, nos dar· la opcion de "merge by recursive strategy" ya que al haber realizado un git push desde la rama test-1, est· habÌa sido subida al remoto, con contenido editado previamente. El problema esque despuÈs nuestro documento de texto no se encontraba en el repositorio local, por eso hemos tenido que realizar un pull primero, para tenerlos todos sincronizados.
Una vez realizado esto, nuestros commit en rama master pueden ser subidos al repositorio remoto mediante git push. 
=======
Mediante un git pull en el repositorio local, nos dar√° la opcion de "merge by recursive strategy" ya que al haber realizado un git push desde la rama test-1, est√° hab√≠a sido subida al remoto, con contenido editado previamente. El problema esque despu√©s nuestro documento de texto no se encontraba en el repositorio local, por eso hemos tenido que realizar un pull primero, para tenerlos todos sincronizados.
Una vez realizado esto, nuestros commit en rama master pueden ser subidos al repositorio remoto mediante git push. 

>>>>>>> 0d488de71eb8dedf97a88edd473364fe83ed519f
